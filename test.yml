apiVersion: apps/v1
kind: Deployment
metadata:
  name: user
  namespace: dev
  labels:
    app: user
  # namespace: default
spec:
  selector:
    matchLabels:
      app: user
  replicas: 1
  template:
    metadata:
      labels:
        app: user
    spec:
      containers:
        - name: user
          image: 562114208112.dkr.ecr.us-east-1.amazonaws.com/rawcasterecr@sha256:04c22f320604a9f3b630245291c6a58041ec1e7b1acb7b5738a882707f0101de
          imagePullPolicy: Always
          envFrom:
          - secretRef:
              name: rawcaster-secret-env
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  labels:
    name: user
  name: user
  namespace: dev
spec:
  type: NodePort
  ports:
    - name: user
      port: 80
      targetPort: 80
  selector:
    app: user
